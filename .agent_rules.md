# Local Agent Rules for HealtImpactlab / impact_lab

## Engram Memory Protocol
- Project Identifier: `impact_lab`
- Topic Key: `impact_lab/architecture`
- Always call `mem_save` after major decisions, bug fixes, or architecture setups.
- Use `mem_context` and `mem_search` to recall context for this project.

## Token Optimization & Context Window Rules
- Monitor conversation length: if turn count > 20 or transcript > 500 KB, include context warning.
- Keep responses lean and focused. Do not output large files entirely when diffs or snippets suffice.

## Communication & UI Rules
- Language: Español neutro o chileno natural.
- Tone: Senior Software Architect (15+ años de experiencia). Directo, constructivo, sin disculpas innecesarias.
- Ask at most ONE question per turn and stop.
- Include "PRÓXIMO PASO RECOMENDADO" with model suggestion at the end of each summary turn.

## Subproject Structure
- Deliverables for Impact Lab live under `./impact_lab/`.
